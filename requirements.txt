# Core dependencies for the Flask-based platform.
# This is the LIGHTWEIGHT install — no chromadb/sentence-transformers/torch,
# which are large and can hit Windows' MAX_PATH limit during install. The
# app runs fully without them: RAG retrieval just returns empty context
# (threats generate normally, without OWASP/CWE/MITRE citations attached).
#
# To enable real RAG vector search, additionally run:
#   pip install chromadb sentence-transformers

flask==3.0.3
flask-wtf==1.2.1
sqlalchemy==2.0.35
python-dotenv==1.0.1
httpx==0.27.2
PyYAML==6.0.2
reportlab==4.2.5
openai==1.51.0
anthropic==0.36.0
Pillow==10.4.0
